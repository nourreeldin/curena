package com.fueians.medicationapp.model.security
/**
 * PasswordHasher handles all password encryption and verification operations.
 *
 * Notes for developers:
 * - Use secure hashing algorithms (e.g., BCrypt or SHA-256 with salt) to protect passwords.
 * - Do NOT store or log plain-text passwords at any point.
 * - Keep hashing and verification logic centralized here for reuse across the app.
 */
class PasswordHasher {
    // TODO: Implement hashing and verification methods
}
