package com.fueians.medicationapp.model.repository;
/**
 * UserRepository manages all operations related to user data handling.
 *
 * Notes for developers:
 * - Use Dagger Hilt for dependency injection to provide data sources.
 * - Responsible for communication between local (SQLite/Room) and remote (Supabase) storage.
 * - Keep this class focused on handling data.
 */
class UserRepository {
    // TODO: Define data operations and integrate local/remote sources
}