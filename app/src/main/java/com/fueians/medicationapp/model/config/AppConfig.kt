package com.fueians.medicationapp.model.config
/**
 * AppConfig is the centralized configuration class for the application.
 *
 * Notes for developers:
 * - Keep all constants organized and grouped logically (e.g., API, Database, UI).
 * - Avoid hardcoding sensitive data like real API keys. Use environment variables or secure storage.
 * - Update values here when integrating new services (e.g., Supabase, SQLite, or third-party APIs).
 * */
class AppConfig {
    // TODO: Define constants such as BASE_URL, and API_KEY here.
}